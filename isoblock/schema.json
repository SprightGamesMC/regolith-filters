{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "isoblock",
    "type": "object",
    "properties": {
        "resolution": {
            "type": "number",
            "default": 128,
            "description": "Width and height in pixels of each generated image."
        },
        "outputPath": {
            "type": "string",
            "default": "build/isoblock",
            "description": "Output directory for the generated images, resolved from the Regolith project root."
        }
    },
    "additionalProperties": false
}
