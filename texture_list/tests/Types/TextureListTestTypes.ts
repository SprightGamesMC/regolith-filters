/**
 * Shared type definitions for the texture list filter's tests.
 */

/** Layer values written into a fixture texture set. */
export type TextureSetLayerValues = Record<string, string | number[]>;
